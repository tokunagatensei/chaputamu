<%= render 'shared/header' %>
<div class='main'>
  <%# チャットルーム一覧 %>
  <div class="main-rooms">
    <% current_user.rooms.each do |room| %>
      <div class="room">
        <div class="room-name">
          <a href="#"><%= room.room_name %></a>
        </div>
      </div>
    <% end %>
  </div>
  <%# チャットルーム一覧 %>
</div>
