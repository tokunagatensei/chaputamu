<%= render 'shared/header' %>
<div class='main'>
  <%# チャットルーム一覧 %>
  <div class='main-rooms'>
    <% current_user.rooms.each do |room| %>
      <div class='room'>
        <div class='room-name'>
            <%= link_to room.room_name, '#'%>
        </div>
      </div>
    <% end %>
  </div>
  <%# チャットルーム一覧 %>
</div>
